			SELECT SELF;
			FORALL CREEP
			(
				VAR_BOUNDS SELECTION ((GET CREEP_TYPE SELF) - 1) 4 32;
				ROTATE SELECTION (270 - (ZERO_ANGLE_D (GET POSITION SELF) (GET MOVE_TARGET SELF))) 0 0;
				DRAW SELECTION (GET POSITION SELF);
			);


			SELECT SELF;
			FORALL TOWER
			(
				IF ((CAST_CHECK SELF) == FALSE)
				(
					VAR_BOUNDS SELECTION (GET TOWER_TYPE SELF) 21 48;
					DRAW SELECTION (GET POSITION SELF);
				)
				ELSE
				(
				);
			);

			SELECT SELF;
			FORALL TOWER
			(
				IF (((CAST_CHECK SELF) == FALSE) &amp;&amp; ((GET TOWER_TYPE SELF) &gt; 1))
				(
					SET POSITION SELECTION (POINT2D ((GET POS_X SELF) + 2) ((GET POS_Y SELF) + 25));
					FOR (GET TOWER_LEVEL SELF)
					(
						DRAW SELECTION (GET POSITION SELECTION);
						SET POSITION SELECTION (POINT2D ((GET POS_X SELECTION) + 6) (GET POS_Y SELECTION));
					);
				);
			);


			PRINT_TEXT 12 1 (MAKE POINT2D 836 129) (MAKE POINT2D 0 0) (MAKE POINT2D 180 1) (TRUNCATE (GET GAME_INK WORLDGAME)) / (GET GAME_MAX_INK WORLDGAME);

			PRINT_TEXT 18 1 (POINT2D 780 212) (POINT2D 0 0) (POINT2D 64 1) (GET GAME_WAVE_NUMBER WORLDGAME);